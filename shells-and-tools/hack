#!/bin/bash

CTFNAME=narnia
SYSTEM=$CTFNAME.labs.overthewire.org
PORT=2226
USER=$1 

echo "Logging into " $SYSTEM":"$PORT " as " $USER

PASSFILE=$CTFNAME.txt

echo "Password for $USER is" 
grep $USER $PASSFILE | cut -d ":" -f 2

ssh $USER@$SYSTEM -p $PORT
